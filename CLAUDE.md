# CLAUDE.md

給後續 session 的專案交接文件。**開始動手前請完整讀過**，特別是「不要做的事」那一節——裡面每一條都是已經討論定案的決策，不要重新提案。

---

## 這是什麼

教長輩使用手機常見 App 的**沙盒模擬器**。使用者在一個假的通訊軟體介面裡練習，怎麼按都不會有真實後果。

第一個標的是台灣的通訊軟體（LINE），目前完成一課：傳語音訊息。

**目標使用者**：68 歲以上、老花或白內障、手抖、識字有限、對手機沒信心。次要使用者是子女與社區據點的志工。

---

## 第一原則（所有設計決策都從這兩條推導）

**一、這裡不管你怎麼按都不會有後果。**
長輩學不會手機的主因不是理解力，是怕：怕按壞、怕轉錯錢、怕在家族群組出糗、怕被笑。產品最大的價值是消除後果，不是提供步驟說明。

推論：不索取權限、沒有分數、沒有紅色錯誤提示、沒有計時、錯誤路徑全部放行。

> **2026-09-13 更新，「不要註冊登入」已被使用者明確推翻。** 原始推論是「不要註冊登入」，
> 理由不變（登入畫面對長輩等同釣魚頁）；但使用者在知情、確認過這個風險後，
> 決定 App 全站改為強制 LINE 登入（未登入會被導去 LINE 的授權同意畫面）。
> 這不是 AI 自己重提，是使用者本人的決定，後續 session 不要「修正」回無登入版本。
> 詳見 `src/auth/lineAuth.web.ts`、`src/shell/LoginGate.tsx`。

**二、長輩靠空間記憶學介面，不是靠概念模型。**
他們記的是「綠色那顆在右下角」，不是「這是傳送鍵」。

推論：模擬畫面必須跟真實 App 幾乎一模一樣。**為了好按而放大按鈕或簡化版面，會直接毀掉回到真手機的遷移效果**——這是這類產品最常見的失敗原因。要解決按不到的問題，正確做法是另外做一課教他調系統字級，不是改模擬畫面。

**唯一的成功指標是遷移**：他在自己手機上、沒有提示的情況下能不能獨立完成。不是 App 內完課率。

---

## 技術現況

- Expo SDK 57 / React Native 0.86 / React 19 / TypeScript
- **零額外相依套件**，刻意如此，`npm install` 一次就能跑
- 已驗證：`npx tsc --noEmit` 無錯，`npx expo export --platform android` 打包成功（589 modules）

```bash
npm install
npx expo start        # a = Android, i = iOS
npx tsc --noEmit      # 改完務必跑
```

網頁版（拿去社區據點測試用，免安裝）：
```bash
npx expo install react-dom react-native-web @expo/metro-runtime
npx expo start --web
```

---

## 架構

```
App.tsx                 路由 + 鷹架階段推進 + 教學外框
src/engine/types.ts     腳本引擎的資料結構 ← 真正的資產
src/content/lessons.ts  課程腳本 + 路線圖資料
src/sim/                被模擬的 App（parts.tsx 元件、ChatSim.tsx 容器）
src/shell/              教學外殼：MapScreen、LessonScreens、BigButton
src/ui/                 theme.ts、Scale.tsx、Icons.tsx
```

### 分層規則（請嚴格遵守）

**`sim/` 是純呈現層，只吃 props，完全不知道「課程」存在。** 換一個 App = 換 `sim/` 元件 + 加一批 `content/` 腳本，`engine/` 和 `shell/` 不動。任何把課程邏輯洩進 `sim/` 的改動都要擋掉。

**引擎 + 腳本，不要寫死畫面。** 畫面、狀態、轉場、語音稿都用宣告式資料描述。新增一個 App 不該需要改程式碼。長期資產是腳本編輯工具和教案庫，不是那幾個畫面。

**平台差異放在資料裡，不要散落 `Platform.OS` 判斷。** `Lesson` 型別已預留 `screens.ios` 覆寫的位置（尚未實作）。iOS 與 Android 的真實 App 在存照片、分享、返回行為上差異不小，這些必須各有腳本變體,但不要複製整份課程再改。

### 顏色分層有功能意義

教學外殼用**靛藍** `#1D4E6B`，被模擬的 App 用**綠色** `#12B76A`。這不是美術偏好——長輩要能隨時分辨「藍色是老師，綠色是我要學的東西」。不要混用。

---

## 教學模型：三段鷹架 + 一個里程碑

每個技能跑四關，定義在 `Lesson.stages` 加 `realDevice`：

| 階段 | 提示 | 目的 |
|---|---|---|
| `guided` 帶著做 | 紅圈高亮 + 語音引導條全開 | 第一次接觸 |
| `solo` 自己做 | 無提示，右下角有「卡住了，教我」 | 自己回想 |
| `transfer` 真的做 | 換人、換情境、完全無提示 | 檢驗學到的是操作不是位置 |
| `realDevice` 到真手機 | 離開 App，在自己手機上做一次 | **唯一真正決定成敗的一關** |

**鷹架必須會退場。** 很多教學 App 失敗就是永遠停在第一關，長輩只是學會跟著箭頭按。

`realDevice` 不強制、可跳過，但跳過的人在成效資料裡要分開統計。模擬器裡的成功不會固化記憶，真實互動才會。

---

## 已處理的長輩適用性細節（改動時不要弄壞）

- 主要按鈕最小高度 **72px**
- 麥克風鍵 `hitSlop: 14` + `pressRetentionOffset: 60`，手抖放開時手指滑掉不會失敗
- 長按門檻 `Lesson.target.minMs = 400`，寫在腳本資料裡，真機測試時直接調
- Android `includeFontPadding: false`，所有 `Text` 明確指定 `lineHeight`，否則兩平台中文行高會差好幾像素
- **雙層字級策略**：模擬層 `allowFontScaling={false}`，尺寸只由 `useScale()` 的 App 內參數決定，這樣畫面才跟他真手機一致；外殼層 `systemScaling` 放行系統縮放，讓開了大字的人真的看到大字。統一走 `src/ui/Scale.tsx` 的 `T` 元件，不要直接用 `Text`
- 全面用描邊不用陰影（低對比敏感度看得清楚，也避開 iOS `shadowOffset` 與 Android `elevation` 對不起來）
- 引導動畫尊重 `AccessibilityInfo.isReduceMotionEnabled()`
- 手勢只用「點一下」和「長按」。雙擊、拖曳、滑動對關節退化的人很困難，長按要當獨立技能專門教
- 只點一下麥克風會出現中性糾正「壓著不要放開」，不是錯誤提示

---

## 不要做的事

這些都討論過並否決了。**如果要重提，先問使用者，不要自己加。**

**遊戲化的競爭與稀缺機制，全部不要：**
紅心 / 體力、連續天數 streak、寶石、排行榜、聯賽、限時挑戰、分數、星等、百分比、失敗音效。

理由：長輩會住院、回南部、顧孫、參加喪禮。斷 streak 對年輕人是懊惱，對長輩是「我又搞砸了」，很多人就此不再打開。跟同齡人比數位能力是最直接的羞恥觸發器。

**進度可視化是好的，競爭與稀缺是壞的。** 地圖、勾勾、走過的路變色、完成動畫全部保留。

**不要鎖關卡。** 長輩目標很具體（「我只想跟孫子視訊」），擋住他只會讓他關掉 App 去問女兒。給推薦順序，但任何節點都能進。

**不要做蜿蜒的 Duolingo 式路徑。** 左右擺動、要捲動找下一站對手抖和老花不友善。路徑走直的，節點大、間距大，而且首頁最上方永遠有一顆巨大的「接著上次」，讓他**完全不需要操作地圖也能上課**。地圖是給眼睛看的成就感和給子女看的進度，不是必經的操作關卡。

**不要在使用當下即時呼叫 LLM。**（見下節）

**不要用第三方 App 的商標、logo、圖示或貼圖素材。** 目前所有圖示都是用 `View` 畫的（`src/ui/Icons.tsx`），這是刻意的。

---

## AI 的界線（重要）

**AI 可以生成情境內容，絕對不能生成介面與正確操作路徑。**

UI 佈局、按鈕位置、點擊後的畫面轉場、什麼算「正確答案」，全部必須人工編寫並對照真實 App 驗證。理由：長輩學的是空間記憶，AI 幻覺出一個真實 App 裡不存在的步驟，會教出**錯誤的肌肉記憶**，比沒教更糟，且直接摧毀唯一的成功指標。

**AI 該生成的**：假聯絡人的對話回覆、情境人物與背景、詐騙話術變體、同一技能的多種練習包裝（`transfer` 階段特別適合）。

**但要放在編輯後台，人工審核後打包下發，不是即時生成。** 五個理由：

1. 目標情境是舊 Android + 便宜資費 + 據點爛 Wi-Fi。轉圈五秒對長輩等同當機，他會開始亂點。離線可用是硬需求
2. 機構通路一台平板一天跑十堂課乘上百個據點，即時呼叫是會隨規模線性成長的 opex，補助款專案結案後就死了
3. 第一次學新技能時情境每次都不一樣會干擾記憶固化。**變異性是用來檢驗遷移的，不是用來教新東西的**——所以 `guided`/`solo` 固定腳本，只有 `transfer` 和複習用變體
4. 台語 TTS 品質目前不到能教長輩的程度，語調怪一點他就聽不懂。預先生成的腳本可以請人真人錄音
5. 防詐內容必須人審。165、官方帳號名稱、求助管道絕不能是生成的

唯一可考慮即時的是假聯絡人的自由聊天，且必須有退回罐頭回覆的離線路徑。

---

## 內容規劃

**LINE 教學順序**（依實際使用價值排，不是依功能複雜度）：
讀訊息與回覆 → 貼圖 → **語音訊息** → 語音／視訊通話 → 傳照片與存照片 → 群組與一對一的差別 → 加好友 → 辨識官方帳號與真人 → 封鎖退群檢舉 → LINE Pay（最後，且保守）。

語音訊息和視訊要往前排：前者繞過打字這道最大的牆，後者是他們願意學手機的真正理由。

**防詐模組是獨立核心賣點**，不是附屬功能。在沙盒裡收假訊息，練習「停、掛、問、打165」這些**動作**。教可遷移的判準（急、密、錢、連結）而不是背案例——警告過度會讓長輩乾脆什麼都不敢用，或只背下特定話術、換個劇本就中招。

**識字與輸入法**：注音和拼音是完全的牆。語音輸入和語音訊息要排在打字之前；真要教打字，優先教**手寫輸入**。

---

## 待辦（依優先序）

1. **語音播放**。目前「聽我念一次」只切換文字狀態，沒有音檔。台語真人錄音是核心資產，**必須打包進 App 不要串流**。國語與台語都要，字幕永遠開著，語速可調
2. **第二課（視訊通話）**。這會逼出腳本格式的所有問題，趁現在只有一課的時候改最便宜
3. **進度持久化**（AsyncStorage）
4. **iOS 畫面變體**，`Lesson` 型別已預留位置
5. 首次啟動的字級選擇流程（決定整個模擬器的呈現尺寸）
6. 子女端／照顧者模式：遠端指派、看進度、收「媽媽學會視訊了」通知。這是動機來源，也是唯一現實的付費來源——長輩幾乎不會為 App 付錢，子女會
7. 志工帶班模式：班級進度看板、**可列印的紙本講義**（長輩非常依賴紙本筆記，別忽略）

---

## 非技術風險（會影響技術決策，別忽略）

**法務**：複製介面外觀涉及商標與著作權。實務對策是自繪近似素材、絕不用他人 logo 做識別、全程標示「非官方，僅供教學」。但**更好的做法是主動去談官方合作**——LINE 台灣本身就在做數位素養推廣，這能把最大的法律風險換成最大的通路資產。

**iOS 審查風險明顯高於 Android。** Apple 對「外觀模仿其他 App」相當敏感。務必規劃「iOS 被卡住時 Android 照樣上線」的路徑，不要讓兩個平台綁死彼此時程。測試階段走 EAS 側載 APK 和 TestFlight，完全繞過商店審查。

**版本漂移**：真實 App 一直改版，複製品會持續腐化。對策是宣告式腳本（改腳本比改程式碼便宜十倍）、課程集中在穩定功能（訊息、貼圖、通話）而避開易變的角落、把維護當常態預算而非意外。

**安裝的雞生蛋問題**：需要學 App 的人往往裝不起教學 App。靠機構預裝、志工協助首次開啟、子女代裝、以及**免安裝的網頁版**。

---

## 給 session 的行為準則

**改動前**：確認沒有違反「不要做的事」和 AI 界線那兩節。

**改動後**：跑 `npx tsc --noEmit`。改到排版或字級相關的，兩個平台都要看。

**遇到範圍問題**：這個專案很容易失控（地圖、AI 管線、雙平台、像素級模擬、台語語音）。優先序是 **模擬器保真度 > 語音 > 三段鷹架 > 地圖 > 雙平台 > AI 生成**。前三項決定「他到底有沒有學會」。如果使用者的要求會擴大範圍，先講清楚代價再動手。

**不要在模擬器上驗證就宣稱完成。** 模擬器測不出手指乾燥點不到、老花看不清、握持姿勢這些真正會殺死產品的問題。任何 UI 改動都應該建議在入門紅米或三星 A 系列實機上確認。

**回應語言**：繁體中文（台灣用語）。